'use client'

import useDictionary from '@/utils/hooks/useDictionary'

export default function FooterContactUs() {
    const dict = useDictionary()

    return (
        <div>
            <a href="mailto:hello@mshrm.studio">hello@mshrm.studio</a>
        </div>
    )
}
